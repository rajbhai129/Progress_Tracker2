<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Progress Tracker - Empower Your Learning</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    /* CSS Reset */
    
     
      
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      width: 100%;
      overflow-x: hidden;
    }

    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
     
    }

    /* Header & Navigation */
    .landing-header {
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      color: #fff;
      width: 100%;
      position: relative;
    }

    .navbar {
      padding: 15px 20px;
    }

    .nav-container {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo h2 {
      font-size: 24px;
      margin: 0;
    }

    .menu-toggle {
      display: none;
      background: none;
      border: none;
      color: white;
      font-size: 24px;
      cursor: pointer;
      padding: 5px;
    }

    .nav-links {
      display: flex;
      list-style: none;
      gap: 15px;
      align-items: center;
    }

    .nav-links a.btn {
      display: inline-block;
      padding: 8px 20px;
      background: white;
      color: #2575fc;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .nav-links a.btn:hover {
      background: #f0f0f0;
      transform: translateY(-2px);
    }

    /* Hero Section */
    .hero {
      text-align: center;
      padding: 60px 20px;
    }

    .hero-content h1 {
      font-size: 2.8rem;
      margin-bottom: 20px;
    }

    .hero-content p {
      font-size: 1.2rem;
      margin-bottom: 30px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    #get-start-btn {
      padding: 15px 30px;
      font-size: 1.1rem;
      background: #ff7f50;
      color: white;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    #get-start-btn:hover {
      background: #ff6333;
      transform: scale(1.05);
    }

     /* 🌟 Dashboard Button (After Login) */
    #dashboard-btn {
      padding: 15px 30px;
      font-size: 1.1rem;
      background: #e325fc; /* Blue Theme */
      color: white;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    #dashboard-btn:hover {
      background: #911ab8;
      transform: scale(1.05);
    }


    /* Features Section */
    .features-wrapper {
      background: white;
      padding: 60px 20px;
      text-align: center;
    }

    .features-wrapper h2 {
      font-size: 2.5rem;
      margin-bottom: 40px;
    }

    .feature-container {
      display: flex;
      justify-content: center;
      gap: 30px;
      max-width: 1200px;
      margin: 0 auto;
      flex-wrap: wrap;
    }

    .feature {
      flex: 1 1 300px;
      max-width: 350px;
      padding: 30px;
      background: #f8f9fa;
      border-radius: 10px;
      box-shadow: 0 2px 15px rgba(0,0,0,0.1);
    }

    .feature i {
      font-size: 2.5rem;
      color: #2575fc;
      margin-bottom: 20px;
    }

    .feature h3 {
      margin-bottom: 15px;
      font-size: 1.5rem;
    }

    /* About Section */
    .about {
      background: #f4f4f4;
      padding: 60px 20px;
      text-align: center;
    }

    .about h2 {
      font-size: 2.5rem;
      margin-bottom: 20px;
    }

    .about p {
      max-width: 800px;
      margin: 0 auto 30px;
      font-size: 1.1rem;
    }

    .about button {
      padding: 12px 25px;
      background: #2575fc;
      color: white;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      font-size: 1rem;
      transition: all 0.3s ease;
    }

    .about button:hover {
      background: #1a5bb8;
      transform: scale(1.05);
    }

    /* Footer */
    .footer {
      background: #333;
      color: white;
      padding: 20px;
      text-align: center;
    }

    .social-links {
      margin-top: 15px;
    }

    .social-links a {
      color: white;
      margin: 0 10px;
      font-size: 1.2rem;
      transition: color 0.3s ease;
    }

    .social-links a:hover {
      color: #ff7f50;
    }
    /* 🌟 Preloader Styling */
    #preloader {
      position: fixed;
      width: 100%;
      height: 100%;
      background: white;
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1000;
      transition: opacity 0.5s ease-out;
    }
    
    /* Hide preloader smoothly */
    #preloader.hidden {
      opacity: 0;
      pointer-events: none;
    }
    

.spinner {
  width: 60px;
  height: 60px;
  border: 5px solid #6a11cb;
  border-top: 5px solid transparent;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* 🌟 Smooth Fade-in Effect */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 1s ease-out, transform 1s ease-out;
  will-change: opacity, transform;
  pointer-events: none; /* Prevents interaction issues */
}

/* When element becomes visible */
.fade-in.show {
  opacity: 1;
  transform: translateY(0);
  pointer-events: auto;
}



    /* Mobile Responsive Styles */
    @media (max-width: 768px) {
      .menu-toggle {
        display: block;
      }

      .nav-links {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: #2575fc;
        padding: 20px;
        flex-direction: column;
        width: 100%;
        z-index: 1000;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      }

      .nav-links.active {
        display: flex;
      }

      .nav-links li {
        width: 100%;
        margin: 8px 0;
      }

      .nav-links a.btn {
        display: block;
        width: 100%;
        text-align: center;
        padding: 12px 20px;
      }

      .hero-content h1 {
        font-size: 2rem;
      }

      .hero-content p {
        font-size: 1rem;
        padding: 0 15px;
      }

      .features-wrapper, 
.about {
  color: #333 !important; /* Ensure text color remains visible */
}


      .feature {
        margin: 0 15px;
      }
    }


    .features-wrapper, 
    .about, 
    .features-wrapper h2, 
    .features-wrapper p, 
    .about h2, 
    .about p {
      color: #333 !important; /* Force text color */
      transition: color 0.3s ease-in-out;
    }
    


  </style>
</head>
<body>
  <!-- 🌟 Preloader Animation -->
<div id="preloader">
  <div class="spinner"></div>
</div>

  <!-- Header & Navigation -->
  <header class="landing-header">
    <nav class="navbar">
      <div class="nav-container">
        <div class="logo">
          <h2>Progress Tracker</h2>
        </div>
        <button class="menu-toggle" id="mobile-menu">
          <i class="fas fa-bars"></i>
        </button>
        <ul class="nav-links" id="nav-links">
          <li><a href="#about" class="btn">About</a></li>
          <% if (user) { %>
            <li><a href="/dashboard" class="btn">Dashboard</a></li>
            <li><a href="/logout" class="btn">Logout</a></li>
          <% } else { %>
            <li><a href="/login" class="btn">Login</a></li>
            <li><a href="/register" class="btn">Register</a></li>
          <% } %>
        </ul>
        
      </div>
    </nav>

    <!-- Hero Section -->
   <!-- Hero Section with Animation -->
<div class="hero fade-in">
  <div class="hero-content">
    <h1>Empower Your Learning Journey</h1>
    <p>Track your progress, stay motivated, and achieve your goals with our intuitive progress tracker.</p>
    
    <% if (user) { %>
      <button id="dashboard-btn" onclick="window.location.href='/dashboard'">Go to Dashboard</button>
    <% } else { %>
      <button id="get-start-btn" onclick="window.location.href='/register'">Get Started</button>
    <% } %>
  </div>
</div>

<!-- Features Section -->
<section class="features-wrapper fade-in">
  <h2>Our Key Features</h2>
  <div class="feature-container">
    <div class="feature">
      <i class="fa-solid fa-chart-line"></i>
      <h3>Real-Time Analytics</h3>
      <p>Visualize your learning progress with interactive charts and statistics.</p>
    </div>
    <div class="feature">
      <i class="fa-solid fa-lightbulb"></i>
      <h3>Smart Insights</h3>
      <p>Receive personalized recommendations to keep you on track.</p>
    </div>
    <div class="feature">
      <i class="fa-solid fa-user-check"></i>
      <h3>Easy Onboarding</h3>
      <p>Sign up quickly and start tracking with an intuitive dashboard.</p>
    </div>
  </div>
</section>

<!-- About Section -->
<section class="about fade-in">
  <h2>About Us</h2>
  <p>At Progress Tracker, we believe that tracking progress is key to success.</p>
  <button onclick="window.location.href='/register'">Join Us Today</button>
</section>


  <!-- Footer -->
  <footer class="footer">
    <p>&copy; <%= new Date().getFullYear() %> Progress Tracker. All rights reserved.</p>
    <div class="social-links">
      <a href="#"><i class="fab fa-facebook-f"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-instagram"></i></a>
    </div>
  </footer>

  <script>
    // Mobile menu toggle
    const mobileMenu = document.getElementById('mobile-menu');
    const navLinks = document.getElementById('nav-links');
    
    mobileMenu.addEventListener('click', function(e) {
      e.stopPropagation();
      navLinks.classList.toggle('active');
    });
    
    // Close menu when clicking outside
    document.addEventListener('click', function(e) {
      if (!navLinks.contains(e.target) && !mobileMenu.contains(e.target)) {
        navLinks.classList.remove('active');
      }
    });

    // Close menu when clicking a link
    document.querySelectorAll('.nav-links a').forEach(link => {
      link.addEventListener('click', function() {
        navLinks.classList.remove('active');
      });
    });

    // Remove Preloader when Page Loads
    window.addEventListener("load", () => {
      const preloader = document.getElementById("preloader");
      preloader.classList.add("hidden"); // Apply transition
      setTimeout(() => {
        preloader.style.display = "none";
      }, 500); // Wait for transition to complete
    
      // Show animated sections
      document.querySelectorAll(".fade-in").forEach((el, index) => {
        setTimeout(() => {
          el.classList.add("show");
        }, index * 300);
      });
    });
    
  </script>
</body>
</html>